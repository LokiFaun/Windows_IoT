<Page
    x:Class="Dashboard.View.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Dashboard.View"
    xmlns:viewModels="using:Dashboard.ViewModel.DesignTime"
    xmlns:converter="using:Dashboard.Converter"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Page.Resources>
        <converter:DateConverter x:Key="DateConverter"/>
        <converter:TimeConverter x:Key="TimeConverter"/>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Row="0" 
                    Grid.Column="0" >
            <TextBlock Text="{Binding Path=CurrentDateTime, Converter={StaticResource DateConverter}}"
                       FontSize="70"/>
            <TextBlock Text="{Binding Path=CurrentDateTime, Converter={StaticResource TimeConverter}}"
                       FontSize="70"
                       FontFamily="Consolas"/>
        </StackPanel>
    </Grid>
</Page>
